import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SchedulePickerPageComponent } from './schedule-picker-page.component';

describe('SchedulePickerPageComponent', () => {
  let component: SchedulePickerPageComponent;
  let fixture: ComponentFixture<SchedulePickerPageComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SchedulePickerPageComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulePickerPageComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
