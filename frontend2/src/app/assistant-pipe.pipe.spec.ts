import { AssistantPipePipe } from './assistant-pipe.pipe';

describe('AssistantPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AssistantPipePipe();
    expect(pipe).toBeTruthy();
  });
});
